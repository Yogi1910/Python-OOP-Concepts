# Exercises

1. Implement a `Playlist` class that supports adding/removing tracks and exposes iteration.
2. Extend the `Account` example with withdrawal limits and overdraft protection.
3. Model a `Shape` hierarchy with area calculations and add unit tests under `tests/`.
4. Build a `Command` pattern for undoable text editor operations.
